# 工作流与 Prompt 设计通用经验 · 技能记忆

每次使用此技能后，请在此追加使用记录，包括：
- 本技能在哪个场景下被触发
- 技能内容是否覆盖了实际需求（哪些够用、哪些不够）
- 使用过程中发现的新边界案例或反模式
- 对技能本身（原则/自检/方法论）的改进建议

---

## 2026-06-09 创建与初始填充

- **场景**：将 `设计工作流元经验.md` 中的原则提炼为 Agent 可自动加载的技能
- **版本**：v1 —— 5 条核心原则 + 8 项快速自检清单
- **结构**：沿用 skill.md 标准格式（frontmatter triggers + 主体内容）
- **后续回流**：
  - 心理力学行为改变公式 → §五 5.1（正面方法论）
  - 心理力学 vs MDA 区分 → §五 5.2 + §四 4.2（正+负）
  - 强化程式单一依赖 → §四 4.1（负面自检）
  - 信号包装法 → 曾加入 §五 后因「不属于游戏设计」被移除
- **边界案例**：§四（经验教训）和 §五（方法论）是本次新增的概念——需确认后续其他技能文件是否也采用同样的 §四/§五 结构

## 2026-06-09 MUSE-Autoskill 论文分析发现技能级记忆差距

- **场景**：分析 arXiv:2605.27366 时对比本技能与 MUSE 的 skill lifecycle
- **发现**：本技能此前没有 `.memory.md`，每次使用无历史经验可查
- **改进**：创建本文件，成为第一个拥有技能级记忆的技能文件
- **待决**：是否为本技能添加 `tests/` 目录（见论文分析，当前优先级不高）

## 2026-06-09 知识回流实战：调研报告见解入库

- **场景**：对《调研报告_反馈信号驱动力心理力学.md》执行知识回流
- **变更**：§四 新增 4.1（强化程式单一依赖）、4.2（心理力学vsMDA混淆）；§五 新增 5.1（心理力学行为改变公式）、5.2（心理力学是思考基点）
- **学到的**：
  - guidance_type（positive/negative/both）机制有效防止了"所有知识都变成自检清单"的问题
  - §五 作为新概念需要在其他技能中普及（目前只有本技能有 §五）
  - 信号包装法虽被移除，但其"降门槛前置"思想可迁移到其他设计场景

## 2026-06-09 Skill 编写规范审计优化

- **场景**：用户要求用 Skill 编写规范检查并优化本技能
- **自检发现**：
  - Frontmatter 缺 `version`/`layer`/`estimated_tokens` → 已补充（version: 1.0.0, layer: domain, estimated_tokens: 6000）
  - 缺 🪶 鸟瞰摘要 → 新增「🪶 速查」表（8 行，覆盖所有章节导航）
  - triggers 不完整 → 从 `_registry.json` 同步了 `审查工作流`/`审查prompt`/`review`/`审核`/`审计技能` 共 5 个
  - §四 只有反模式无不适用场景 → 新增 4.3 不适用场景说明（4 类）
  - §五 缺决策启发式 → 新增 5.3 设计决策启发式表（7 行"如果目标...则..."）
  - tools 偏少 → 新增 `create_file`/`replace_string_in_file`
- **registry 差异修复**：`_registry.json` 的 tools 列表已同步
- **边界案例**：`_registry.json` 的 `estimated_tokens: 12000` 远大于本技能实际大小（~250 行≈6000 tokens）。实际值与注册表中的值不一致——注册表可能是预先填的大值，未按文件实际大小重新估算。当前优先保证技能文件正确，注册表后续再统一修正
